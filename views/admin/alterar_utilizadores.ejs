<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/form.css" />
  </head>
  <body>
    <div class="container">
      <h1>Alterar Cliente</h1>
      <form action="/admin/utilizadores/alterar" method="POST">
        <input type="hidden" name="_id" value="<%= cliente._id %>">
        <label for="email">Email</label>
        <input type="email" id="email" name="email" placeholder="Email" value="<%= cliente.email%>" required/>
        <label for="password">Password</label>
        <input
          type="password"
          id="password"
          name="password"
          placeholder="Password" value="<%= cliente.password%>" required
        />
        <label for="nome">Nome</label>
        <input type="text" id="nome" name="nome" placeholder="Nome" value="<%= cliente.nome%>" required/>

        <label for="nif">Numero Identificaçao Fiscal</label>
        <input type="number" id="nif" name="nif" placeholder="NIF" minlength="8" maxlength="8" value="<%= cliente.nif%>" required/>

        <label for="nome">Data de nascimento</label>
        <input type="date" id="data_nascimento" name="data_nascimento" placeholder="Data" value="<%= cliente.data_nascimento%>" required/>

        <button type="submit">Alterar Cliente</button>
      </form>
    </div>
  </body>
</html>
